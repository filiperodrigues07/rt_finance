# RT Finance Web — o frontend só conhece a URL da API.
# Em dev o Vite faz proxy de /api → http://localhost:3333 (veja vite.config.ts).
# Para apontar para outra API em dev:
VITE_API_TARGET=http://localhost:3333
